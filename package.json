{
  "name": "mkpasswdpwa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "spago bundle-app --to docs/app.js",
    "watch": "spago bundle-app --watch --to docs/app.js",
    "serve": "http-server ./docs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "amderbar",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.11.1",
    "spago": "^0.8.0"
  },
  "dependencies": {
    "purescript": "^0.12.5"
  }
}
