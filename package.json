{
  "name": "mkpasswdpwa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "spago bundle-app --to docs/app.js",
    "watch": "spago bundle-app --watch --to docs/app.js",
    "serve": "web-dev-server --node-resolve --app-index docs/index.html --watch",
    "test": "spago test"
  },
  "author": "amderbar",
  "license": "MIT",
  "devDependencies": {
    "@web/dev-server": "^0.1.17",
    "purescript": "^0.14.2",
    "purty": "^7.0.0",
    "spago": "^0.20.3"
  }
}
