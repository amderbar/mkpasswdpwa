{
  "name": "mkpasswdpwa",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "spago bundle-app --main Main.SPA --to docs/app.js --minify",
    "precli": "spago bundle-app --main Main.CLI --to bin/cli.mjs --minify --platform=node",
    "cli": "node bin/cli.mjs",
    "watch": "npm run build -- --watch",
    "serve": "wds --node-resolve --app-index docs/index.html --root-dir docs --watch",
    "test": "spago test"
  },
  "author": "amderbar",
  "license": "MIT",
  "devDependencies": {
    "@web/dev-server": "^0.1.17",
    "esbuild": "^0.17.5",
    "purescript": "^0.15.0",
    "purty": "^7.0.0",
    "spago": "^0.20.3"
  }
}
