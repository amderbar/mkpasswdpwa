{
  "name": "mkpasswdpwa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "spago bundle-app --to docs/app.js",
    "watch": "spago bundle-app --watch --to docs/app.js",
    "serve": "live-server --no-browser ./docs",
    "test": "spago test"
  },
  "author": "amderbar",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.1",
    "purescript": "^0.13.3",
    "spago": "^0.15.2"
  }
}
